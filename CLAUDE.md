# 프로젝트 컨텍스트 (Claude Code 핸드오프)

금융지주회사 내부통제시스템 RAG 프로젝트의 **그래프DB 구축** 부분. 최종
목표는 대한민국 법령(현재 33개 금융권 관련 법령)을 조문 단위 지식그래프로
만들어, 사내 내부통제기준/통제항목이 어떤 법조문에 근거하는지 추적 가능한
GraphRAG를 만드는 것.

**핵심 방향 전환**: 초반에는 LLM 기반 엔티티/관계 추출을 검토했으나,
컴플라이언스 시스템 특성상 "왜 이 관계가 추출됐는지 설명 가능해야 한다"는
이유로 **NLP 형태소분석 기반 결정론적 추출**로 전환했다. 지금 코드베이스에는
이 전환 이전의 산출물이 일부 남아있으니 아래 "폐기된 부분" 섹션을 꼭 읽을 것.

## 지금 당장 알아야 할 것: 폐기된 코드

다음 파일들은 **LLM 기반 추출 + 범용 도메인 스키마(v1)** 시절 산출물이고,
현재 진행 중인 NLP 파이프라인과 무관하다. 새 작업은 이 파일들을 참고하지
말 것 (그대로 남겨둔 이유는 삭제 결정을 아직 안 내렸기 때문 — 필요 없다고
판단되면 지워도 됨):

- `src/graphdb/schema.py`, `src/graphdb/models/entities.py` — Company/
  Department/Regulation/ControlItem/Risk/Process 같은 범용 스키마. 법령
  조문 중심 스키마로 다시 설계해야 함 (아직 미착수, 아래 "다음 단계" 참고).
- `src/graphdb/ingest/document_extractor.py` — LLM(vLLM/Qwen)으로 문서에서
  엔티티/관계를 뽑는 코드. **폐기됨.** 재사용 안 함.
- `src/graphdb/ingest/domain_loader.py`, `data/domain/sample_internal_control.yaml`
  — 위 범용 스키마용 샘플 데이터 로더. 마찬가지로 폐기 대상.
- `tests/test_schema.py`, `tests/test_domain_loader.py` — 위 폐기 코드에
  대한 테스트라 지금은 그린이지만 의미 없음.

**계속 유효한 인프라**: `docker-compose.yml`(Apache AGE), `sql/init_graph.sql`,
`src/graphdb/config.py`, `connection.py`, `cypher.py` — AGE 연결/Cypher 실행
레이어는 스키마와 무관하게 재사용 가능. 아직 실제로 그래프에 뭘 적재해본
적은 없음(파이프라인이 아직 그 단계 전).

## 지금까지 완료된 파이프라인 (NLP 직접추출, v2)

```
법령 원문 수집 → 형태소분석(동사 파악) → 동사 그룹핑   ← 지금 여기까지
→ [미착수] 의존구문분석(주어/목적어+양상 추출) → [미착수] 엔티티 정규화
→ [미착수] 그래프 스키마 확정 + AGE 적재
```

### 1단계 — 법령 원문 수집 (완료)
- `data/laws/law_list.txt`: 사용자가 전달한 33개 법령 원본 목록 (오타/약칭
  그대로 보존 — 감사 추적용).
- `scripts/fetch_laws.py`: [legalize-kr/legalize-kr](https://github.com/legalize-kr/legalize-kr)
  (국가법령정보센터 OpenAPI 미러, 법률/시행령/시행규칙별 md + YAML
  frontmatter) 로컬 체크아웃에서 해당 법령만 복사. 사용법:
  `python scripts/fetch_laws.py /path/to/legalize-kr-checkout`
- `data/laws/raw/`: 복사된 원문 60개 md 파일. `data/laws/manifest.json`:
  파일별 법령MST/공포일자/시행일자/소관부처 메타데이터. 33/33 매칭 완료.

### 2단계 — 형태소분석 (완료)
- `src/graphdb/nlp/morphology.py`: **Kiwi**(kiwipiepy) 사용. 법률문은
  대부분 "체언(NNG/NNP/XR) + 하다/되다(XSV)" 결합이라, 이 결합을 하나의
  "동사구"로 합쳐서 센다 (예: "선임"+"하" → "선임하다"). 의무/금지/재량
  같은 양상은 동사구가 아니라 그 뒤 어미/보조용언 사슬에 실리므로, 여기서는
  분리해서 "양상 후보" 버킷에만 모아둔다.
- `data/laws/domain_dictionary.tsv`: Kiwi 사용자 사전 (법률/금융 전문
  복합명사, 예: 업무집행책임자). 미등록 용어 나올 때마다 계속 추가해야 함.
- `scripts/analyze_verbs.py`: `data/laws/raw/**/*.md` 전체 실행 →
  `data/laws/verb_phrase_frequency.json`(동사구 1,490종/113,140회),
  `data/laws/modal_candidate_frequency.json`(양상 후보 어간 있다/되다/없다,
  7,934회 — 문맥 없이는 의무/금지/재량 셋 중 뭔지 판정 불가해 분리 보관).

### 3단계 — 동사 그룹핑 (완료, v3)
`scripts/group_verbs.py` → `data/laws/verb_groups.jsonl` (그룹당 1줄
JSONL, 1,127개 그룹).

**설계 히스토리 (왜 v3가 됐는지, 다음에 또 뒤엎지 않으려면 필독)**:
- v1: 사람이 "같은 주제"(예: 준수/위반 둘 다 "컴플라이언스 상태") 기준으로
  50개 미만 그룹으로 수작업 매핑. → 사용자가 "준수하다"와 "위반하다"처럼
  **반대 의미**가 한 그룹에 있으면 그래프 relation label만으로 방향(긍정/
  부정)을 알 수 없다고 지적 (컴플라이언스 시스템에서 치명적).
- v2: 반대 의미 그룹을 전부 분리(44→58개, 50개 제약 폐기). 그런데 이번엔
  "보호지원"처럼 반대는 아니어도 **결이 다른 두 의미**(보호 ≠ 지원)를
  묶은 게 또 지적받음.
- v3(현재): 사람의 "비슷하니 묶자" 판단을 아예 없앰. 동사구가 구조적으로
  "한자어 어근 + 파생접미사(하다/되다/받다/시키다)"이므로, **접미사를
  기계적으로 떼어낸 어근으로 자동 그룹핑**. "보호하다"/"지원하다"는 어근이
  달라 자동 분리, "선임하다"/"선임되다"는 어근이 같아 자동 통합(능동/피동
  차이는 아래 "직접 처리해야 할 함정" 참고). 잡았던 버그: "당하다"를
  접미사로 취급하면 "해당하다/정당하다/부당하다/타당하다/상당하다/
  담당하다"처럼 **어근 자체가 "당"으로 끝나는 흔한 한자어**가 전부
  "정하다"류와 오분류됨 — "당하다"를 접미사 목록에서 제거해 수정.

**verb_groups.jsonl 한 줄 예시**:
```json
{"root": "선임", "count": 216, "members": [{"l": "선임하다", "c": 146}, {"l": "선임되다", "c": 70}]}
```

## 다음 단계 (미착수) — 여기부터 이어서 진행

### 4단계: 의존구문분석 기반 주어/목적어 + 양상 추출
- 형태소분석만으로는 주어/목적어를 못 뽑음 — 별도 의존구문분석(Kiwi의
  구문분석 기능 또는 UDPipe-ko 등) 필요.
- **주어 생략(pro-drop) 처리**: "①금융회사는 ...마련하여야 한다. ②제1항에도
  불구하고 ...자회사등은 마련하지 아니할 수 있다" 처럼 항이 이어지며 주어가
  생략되는 경우가 매우 흔함 → 조문 단위로 앞선 항의 주어를 상속하는 규칙
  필요.
- **호/목 열거형 목적어 분리**: "및/·/쉼표" 로 나열된 목적어를 개별
  엔티티로 쪼개는 규칙 필요.
- **양상(의무/금지/재량) 태깅**: 이미 검증된 태그 패턴 사용 —
  의무=`EC(어야)+VX(하)`, 금지=`MAG(아니)+VV(되)`, 재량=`NNB(수)+VA(있)`.
  `modal_candidate_frequency.json`의 있다/되다/없다가 이 판정의 재료.
- **능동/피동 방향 정규화 (중요, 아직 미해결)**: AGE 관계는 방향이 있음
  (`(a)-[r]->(b)`). "설립하다"(능동, 주어=행위자)와 "설립되다"(피동,
  주어=행위대상)를 v3에서 같은 그룹으로 묶었는데, 이걸 그대로 "주어→목적어"
  로 엣지를 만들면 능동/피동에 따라 엣지 방향이 뒤집힌다. 4단계에서 문장의
  능동/피동을 판별해(XSV "하"인지 "되"인지) 피동이면 주어/목적어 역할을
  뒤집어서 항상 "행위자→행위대상" 방향으로 정규화한 뒤 엣지를 만들어야 함.
- REF류 그룹(따르다/관하다/대하다/의하다 등, 전체 동사구 출현의 28%!)과
  DEF류(같다/해당하다/말하다 등)는 실질적 "행위"가 아니라 조문 간
  참조/정의 표현이라, SVO로 뽑기보다 `REFERENCES`/`DEFINED_AS` 같은
  구조적 관계로 바로 보내는 게 맞음 — verb_groups.jsonl의 상위 그룹들이
  대부분 이 케이스이므로 4단계 설계 시 먼저 걸러내야 함.

### 5단계: 엔티티 정규화 + 그래프 스키마 확정 + AGE 적재
- 동일 지시어 통합("금융회사"="해당 회사"="동 회사"), "금융위"="금융위원회"
  같은 약칭 정규화.
- 추출된 명사구를 그래프 NodeLabel로 분류(Regulation/ControlItem/Risk/
  Process/Article 등 — 스키마 재설계 필요, 위 "폐기된 코드" 섹션의 v1
  스키마는 참고만 하고 그대로 쓰지 말 것).
- `src/graphdb/config.py`/`connection.py`/`cypher.py` 레이어를 이용해
  실제 AGE(`docker compose up -d age-db`)에 적재.
- 법령 조문 자체(Article 노드, 위임관계 DELEGATES_TO 등)는 아직 그래프에
  넣어본 적 없음 — `data/laws/raw/*.md`의 `##### 제N조 (제목)` 헤더 +
  `**①**` 항 마커 + "대통령령으로 정한다" 류 위임 문구 파싱이 먼저 필요
  (설계는 논의됐으나 코드는 미작성).

## 환경/실행 방법

- Python 3.12 권장(Docker 기준), `pip install -r requirements-graphdb.txt`
  (이 세션에서 kiwipiepy 등 실제 설치·검증 완료 후 requirements 파일에
  반영함 — 이전에 빠져있었음, 확인 요망).
- `cp .env.example .env` 후 `docker compose up -d age-db` (AGE 그래프DB,
  아직 실사용 안 함).
- 형태소분석 재실행: `PYTHONPATH=src python scripts/analyze_verbs.py`
- 그룹핑 재실행: `python scripts/group_verbs.py`
- 테스트: `PYTHONPATH=src pytest tests/ -q` (위에 언급한 대로 일부는
  폐기된 v1 코드 대상이라 그린이어도 의미 없음)

## 작업 스타일 관련 참고

이 프로젝트는 지금까지 **LLM을 쓰지 않고 결정론적 규칙으로 근거를 코드에
남기는 방식**을 고수해왔다 (감사 추적 가능성이 이유). 4단계 이후에도 이
원칙을 깨지 말 것 — 새 규칙을 추가할 때는 왜 그렇게 판단했는지 주석/커밋
메시지에 남길 것.
